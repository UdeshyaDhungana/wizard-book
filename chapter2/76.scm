
;; In explicit dispatch, existing code must be modified to incorporate new data objects/operations

;; In data directed approach, new type package must be installed

;; In message passing approach, new type must be constructed/operations must be added

;; In a system where new types must be added, data directed approach is suitable, whereas
;; in a system where new operations must be added, message passing is suitable
