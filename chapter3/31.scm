
In an event driven system, any component requires a change in input bit to produce its output.
Upon adding the action procedure, i.e, during initialization, no changes has taken place, so we
need to call the procedure manually.
